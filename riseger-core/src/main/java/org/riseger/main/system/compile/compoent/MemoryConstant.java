package org.riseger.main.system.compile.compoent;

public enum MemoryConstant {
    DATABASE, MAP, SEARCH_SETS, ELEMENT, METOD_PARAMATER, WHERE, RESULT, THRESHOLD, SCOPE, MODELS, AS_TABLE, METOD_PROCESS
}
